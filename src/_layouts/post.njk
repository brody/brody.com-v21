---
layout: base.njk
---

<article>
  {# <div class="my-20 text-center">
    <h1 class="font-bold text-3xl mb-4">{{ title }}</h1>
    <time data-date="{{ page.date }}" datetime="{{ page.date | htmlDateString }}" class="post-date text-textSubtle">{{ page.date | prettyDate }}</time>
  </div> #}

  {# {% if featured_image %}
    <figure class="my-5">
      {% Image "{{ featured_image | url }}",
      "{{ featured_caption }}" %}
      <figcaption class="text-sm px-6 md:text-center">{{ featured_caption }}</figcaption>
    </figure>
  {% endif %} #}

  {# <div class="text-text my-20">
    {{ content | safe }}
  </div> #}

  <div class="entry lg:relative mb-20 md:mb-32 lg:mb-40">
    <time data-date="{{ page.date }}" datetime="{{ page.date | htmlDateString }}" class="mono text-12 text-textSubtle mb-3 inline-block lg:absolute lg:w-40 lg:mt-3 lg:-ml-48 lg:text-right">{{ page.date | prettyDate }}
    </time>

    <h1 class="title text-24 md:text-28 lg:text-32 text-textContrast mb-5">
      {% if link %}
        <a href="{{ link }}" {% if link %}target="_blank"{% endif %}>
          {{ title }}
          <svg class="inline-block h-5 md:h-6 fill-current text-textSubtle" viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg">
            <path fill-rule="evenodd"
                        d="M10.604 1h4.14 -.01c.13-.01.25.11.25.24v4.14l-.01-.01c0 .13-.12.25-.25.25 -.07 0-.14-.03-.18-.08l-1.55-1.55 -3.75 3.75 0-.001c-.31.28-.78.26-1.06-.04 -.27-.29-.27-.74-.001-1.03l3.75-3.75 -1.55-1.55 -.01-.01c-.1-.1-.1-.26-.01-.36 .04-.05.11-.08.17-.08ZM3.74 2l-.01 0c-.97 0-1.75.78-1.75 1.75v8.5c0 .96.78 1.75 1.75 1.75h8.5l-.01-.001c.96 0 1.75-.79 1.75-1.75 0-.01 0-.01 0-.01v-3.5 0c0-.42-.34-.75-.75-.75 -.42 0-.75.33-.75.75v3.5 0c0 .13-.12.25-.25.25h-8.5 -.01c-.14-.01-.25-.12-.25-.26v-8.5l0 0c-.01-.14.11-.26.24-.26h3.5l-.01-.001c.41 0 .75-.34.75-.75 0-.42-.34-.75-.75-.75h-3.5Z"/>
          </svg>
        </a>
      {% else %}
        {{ title }}

      {% endif %}
    </h1>
    {% if featured_image %}
      <figure class="my-5">
        <img src="{{ featured_image }}" alt="{% if figcaption %}{{ title }}{% else %}{{ title }}{% endif %}">
        {% if figcaption %}
          <figcaption>
            {{ figcaption }}
          </figcaption>
        {% endif %}
      </figure>
    {% endif %}
    <article>

      {{ content | safe }}

    </article>
    <div class="flex flex-row w-full justify-between">
      <ul class="tags list-none flex flex-row pl-0">
        {% for tag in tags %}
          <li class="tag">
            <a href="/tags/{{ tag | slug }}">{{ tag }}</a>
          </li>
        {% endfor %}

      </ul>

    </div>
  </div>
</article>